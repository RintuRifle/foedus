# Foedus Backend — PRODUCTION dependencies (512MB free-tier friendly)
#
# Differences from requirements.txt (local dev):
#   ✂ sentence-transformers  — pulls PyTorch (~700MB RAM); prod uses
#                              EMBEDDING_PROVIDER=gemini (API, 0 RAM)
#   ✂ celery / flower        — prod uses TASK_RUNNER=inline (asyncio)
#   ✂ pytest / APScheduler   — tests & daemon mode are dev-only
#     (scraper runs via /scraper/trigger + GitHub Actions cron)

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic[email]==2.10.4
pydantic-settings==2.7.1

sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.1
pgvector==0.3.6

python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20

redis==5.2.1

langchain==0.3.14
langchain-google-genai==2.0.8
langgraph==0.2.60
google-genai>=0.2.0

qdrant-client==1.12.1

PyMuPDF==1.25.2
PyPDF2==3.0.1

httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
aiohttp==3.11.11

loguru==0.7.3
sentry-sdk[fastapi]==2.19.2

slowapi==0.1.9

weasyprint==63.1
Jinja2==3.1.5
markdown==3.7

instructor==1.7.2

python-dotenv==1.0.1
orjson==3.10.13
